import { css } from "lit-element";

export default css`
.nav--container{
    width: fit-content;
    padding: 1.5rem;
    gap: 1rem;
    border-radius: 2rem;
    margin: 0 auto;
}
    .btn-nav{
        
    }
        .btn-nav:hover{
            opacity: 0.8;
        }
        .btn-nav small{
            font-size: 1rem;
        }
`;